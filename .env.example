# Configurações do Supabase
# Obtenha essas informações no painel do Supabase (Settings > API)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Configurações de ambiente
NODE_ENV=production

# IMPORTANTE - SEGURANÇA:
# - Nunca commite o arquivo .env com valores reais
# - Use apenas a chave anônima (anon key) no frontend
# - A service role key deve ser usada apenas no backend/edge functions
# - Mantenha as chaves seguras e rotacione-as regularmente
# - Verifique se o .env está no .gitignore
